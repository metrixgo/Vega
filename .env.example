# Optional: Upstash Redis for cross-device event sync on Vercel (free tier at upstash.com)
# Without these, events persist locally via data/vega_events.json when running `npm run dev`
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
